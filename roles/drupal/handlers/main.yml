---

- name: Restart apache2
  ansible.builtin.service:
    name: apache2
    state: restarted

- name: Restart nginx
  ansible.builtin.service:
    name: nginx
    state: restarted

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

# NOTE: handlers run in the order they are defined here, not the order they are
# notified. Cron is first so that anything it writes is covered by the ownership
# handlers below. It runs as the web server account, as does the systemd timer,
# because bootstrapping Drupal as root leaves a root-owned twig cache under
# sites/default/files that the web server cannot then rewrite.
- name: Run Drupal cron
  ansible.builtin.command:
    cmd: "{{ drupal_drush }} cron"
    chdir: "{{ drupal_base_path }}"
  become: true
  become_user: "{{ webserver_user }}"
  environment: "{{ drupal_drush_env }}"
  changed_when: true

- name: Reset Drupal file ownership
  ansible.builtin.file:
    path: "{{ drupal_base_path }}"
    state: directory
    owner: root
    group: "{{ webserver_group }}"
    recurse: true

- name: Restore Drupal writable directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ webserver_user }}"
    group: "{{ webserver_group }}"
    recurse: true
  loop:
    - "{{ drupal_base_path }}/web/sites/default/files"
    - "{{ drupal_config_sync_path }}"
